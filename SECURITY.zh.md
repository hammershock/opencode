# OpenCode Transit 安全策略

[English](SECURITY.md) · [简体中文](SECURITY.zh.md)

> [!IMPORTANT]
> OpenCode Transit 是基于 [OpenCode](https://github.com/anomalyco/opencode) 构建的独立项目。它并非由 OpenCode 团队开发、认可或维护，与 OpenCode 团队也不存在隶属关系。在这里提交的报告由 Transit 维护者处理，而不是 OpenCode 团队。

## 私密报告漏洞

请使用 GitHub 私密的 [Report a vulnerability](https://github.com/hammershock/opencode-transit/security/advisories/new) 表单。对于尚未公开的漏洞，不要创建公开 Issue、Discussion 或 Pull Request，也不要把秘密放入公开制品。

私密报告应包含：

- 受影响的 `opencode-transit --version` 输出或准确提交；
- 相关时提供控制端平台、Location 类型与目标平台；
- 安全影响，以及被跨越的边界；
- 使用一次性数据的最小可复现步骤；
- 经过清理的日志、截图或概念验证材料；
- 已知规避方案或披露截止时间。

请移除访问令牌、Provider 提示内容、私有主机名、用户名、个人路径、账户标识与真实会话内容。如果某个值是复现所必需的，请先描述它的形态，只有维护者在私密 Advisory 内提出请求后再发送实际值。

如果报告者已经验证行为，能够说明可复现的安全影响，我们欢迎自动化或 AI 辅助分析。只有扫描输出，或没有展示边界跨越的假设性弱点，可能会被视为不可操作而关闭。

## 支持的版本

Transit 正处于积极开发阶段，尚未发布稳定版本线。

| 版本                                             | 安全修复支持   |
| ------------------------------------------------ | -------------- |
| 当前 `dev` 与最新发布的 Transit 预发行版         | 支持           |
| 更早的源码快照、功能分支或上游 `opencode` 二进制 | 不由本项目支持 |

我们可能会请你在当前的干净构建上确认问题。若漏洞位于未改动的上游 OpenCode，可能需要与 [OpenCode 安全流程](https://github.com/anomalyco/opencode/security)协同；Transit 维护者会在不公开报告的前提下决定如何转交。

## 安全模型

OpenCode Transit 是一个有意访问开发工具的 Agent，它不是沙箱。

- Agent 可以使用本地账户或配置的 SSH 账户权限进行读取、写入与执行。
- 权限 UI 是交互与同意边界，不是操作系统级隔离。
- 托管 Rexd 的工作区根目录边界会限制 Transit 通过该 Location 暴露的内容，但不会隔离远程主机账户。
- SSH 主机信任、凭据、网络可达性与操作系统账户策略仍由用户负责。
- 模型 Provider 会依据自己的政策接收用户发送给它们的提示与数据。
- 用户配置的 MCP Server、Plugin、Skill、Transit 之外的 Shell 启动行为与第三方 Provider 属于独立信任边界。
- 百度会话同步没有增加应用层端到端加密，用户需要提供自己的百度应用凭据。
- Server 模式必须由用户主动暴露并配置认证；按用户意图可访问的服务器本身不构成漏洞。

需要隔离不可信代码或数据时，请使用容器、虚拟机或受限的操作系统账户。

## 哪些问题属于范围内

如果报告能展示 Transit 自有边界失效，将尤其有帮助，例如：

- 执行、补全、终端或文件访问发生在错误的 Location；
- 远程操作失败后回退到本地执行；
- 通过 Transit 逃逸受强制执行的工作区根目录或 Skill 资源边界；
- SSH 主机校验或托管 Rexd 安装接受了错误身份或制品；
- Auth 凭据、环境变量值、Provider Token 或同步材料被未授权泄露；
- 会话同步暴露其他账户的数据、复活全局删除的数据、绕过设备成员关系或破坏完整性检查；
- 只读会话或可信命令边界允许了被禁止的修改或 Agent 调用；
- 与文档默认行为相反，启用了未经认证的网络接口；
- 依赖漏洞能够通过受支持的 Transit 路径触达，并造成实质影响。

## 哪些问题通常不属于范围内

除非报告能说明实现违反了文档定义的边界，否则以下情况不属于 Transit 漏洞：

- Agent 使用该操作系统账户执行用户明确允许的动作；
- 没有承诺容器、虚拟机或 OS 沙箱时所称的“沙箱逃逸”；
- 用户配置的模型 Provider、MCP Server、Plugin、Skill、SSH Server 或百度服务的行为；
- 由运行 Transit 的同一用户提供的恶意配置或项目指令；
- 仅耗尽报告者本地资源，且没有持久化或跨用户影响的拒绝服务；
- 不受支持的原生 Windows、Intel Mac、通用 Linux 控制端或过时源码快照行为；
- 社会工程、物理访问，或要求事先完全控制同一 OS 账户的攻击；
- 用户自行放入提示、公开日志、仓库或 Transit 之外其他目标的秘密；
- 没有可复现安全后果的最佳实践建议。

## 协同处理

维护者会在私密 Advisory 中分诊报告、确认受影响边界，并在需要时协调修复与披露。请在 Advisory 发布或维护者同意其他披露日期之前保持细节私密。我们可能会请求你的许可，将报告以保密方式分享给上游 OpenCode 或受影响依赖的维护者。

修复会遵循本项目的验证门禁，包括相关的 Mac 与 WSL2 验收。除非报告者希望匿名，否则 Advisory 会提供贡献署名。
